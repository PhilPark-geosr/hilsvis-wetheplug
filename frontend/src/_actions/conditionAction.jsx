import axios from 'axios';

// get all tidal currents
export const getAllTidalCurrents = () => async (dispatch) => {
  try {
  } catch (err) {
    console.log(err);
  }
};

// select tidal currents
export const selectTidalCurrents = (rows) => async (dispatch) => {
  try {
  } catch (err) {
    console.log(err);
  }
};

// deselect tidal current by id
export const deselectTidalCurrentById = (id) => async (dispatch) => {
  try {
  } catch (err) {
    console.log(err);
  }
};
